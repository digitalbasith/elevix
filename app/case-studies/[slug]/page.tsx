import { notFound } from 'next/navigation';
import Link from 'next/link';
import { ArrowLeft, ArrowUpRight } from 'lucide-react';
import { caseStudies } from '@/lib/content';
import { PageIntro } from '../../components/content-blocks';
import { CallToAction } from '../../components/site-shell';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(c=>c.slug===slug);return {title:c?.title??'Case study not found',description:c?.description}}
export default async function Case({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(c=>c.slug===slug);if(!c)notFound();const index=caseStudies.indexOf(c);const picture=['/images/ai-glass.webp','/images/product-glass.webp','/images/service-cloud.webp'][index];return <main id="main" className="inner-main"><div id="top"/><PageIntro eyebrow="Case Studies" title={c.title} description={c.description}/><section className="container inner-section"><div className="case-detail-hero"><div className={'case-visual case-with-image '+c.color}><img src={picture} alt="Technology concept artwork" loading="lazy"/><span className="case-label">{c.label}</span><div className="case-visual-bottom"><span>{c.tools[0]}</span><ArrowUpRight size={25}/></div></div><div className="case-detail-body"><span className="eyebrow">THE PROJECT</span><h2>Connected technology.<br/>A better way to work.</h2><p>{c.description}</p><div className="tag-list">{c.tools.map(t=><span className="tag" key={t}>{t}</span>)}</div><Link href="/contact-us" className="text-link">Discuss a similar project <ArrowUpRight size={18}/></Link></div></div><div style={{marginTop:40}}><Link href="/case-studies" className="text-link"><ArrowLeft size={16}/>All case studies</Link></div></section><CallToAction/></main>}
